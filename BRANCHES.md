# Branches

Source: private kit TheFLOW (`real-limitless/TheFLOW`). Do not fork these rules here.

| Branch | Audience | What is on it |
| --- | --- | --- |
| `CORE` (this branch) | Everyone | Concept, license, campaign, install pointers. No application source. |
| `DEVELOPMENT` | Operators and developers | Runnable roster-flow. Compose is the supported run path. |

If you only see markdown and no `package.json`, you are still on `CORE`.

```sh
git checkout DEVELOPMENT
```
